\documentclass[a4paper,12pt,amsmath,amssymb,aps,titlepage]{article}
\usepackage[danish]{babel}
\usepackage[utf8x]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{subfiles}

% Preamble imports
\input{preambleParts/dokumentEgenskaber.tex}
\input{preambleParts/grafisk.tex}
\input{preambleParts/fysik.tex}
\input{preambleParts/matematik.tex}
\input{preambleParts/sektioner.tex}
\input{preambleParts/links.tex}
\input{preambleParts/referencer.tex}
\input{preambleParts/spacing.tex}
\input{preambleParts/forside.tex}
\input{preambleParts/badBox.tex}

% Equations may span multiple pages
% \allowdisplaybreaks

% If-statements i LaTeX koden
\usepackage{ifthen} % Tillader if-then statements med \ifthenelse{condition}{thenCode}{elseCode}

\setcounter{secnumdepth}{1} % Vis ikke tal ved sektioner og lignende. Sætter sidste ting, som må have tal: 0 = chapter, -1 = part, 1 = section. Article-klassen har ikke chapters, så 0 er fint.

% Ligninger nummereres 1.1, 1.2 osv. i section 1 og 2.1, 2.2 osv. i section 2
\numberwithin{equation}{section}

\usepackage{tocloft} % Personliggøre inholdsfortegnelse
\renewcommand{\cftsecleader}{\cftdotfill{\cftdotsep}} % Prikker mellem tekst og sidenumre i TOC
% \setcounter{tocdepth}{2} % Kun sektioner og undersketioner i TOC
\setcounter{tocdepth}{4} % Kun sektioner, undersketioner, subsubsec og paragraphs i TOC
\setlength{\cftparaindent}{\cftsubsubsecindent} % Sætter paragraph indent til subsubsec indent i TOC

% Ingen indentering
\setlength\parindent{0pt}

% Bold paragraphs
\newcommand{\boldparagraph}[1]{\paragraph{\textbf{#1}}}

% Enumeration
\usepackage{enumerate}

% To break up equations (aligns) over multiple pages
\allowdisplaybreaks

% Nice matrices with lines in it
\usepackage{tikz}
\newcommand{\tikznode}[2]{%
    \ifmmode%
        \tikz[remember picture,baseline=(#1.base),inner sep=0pt] \node (#1) {$#2$};%
    \else
        \tikz[remember picture,baseline=(#1.base),inner sep=0pt] \node (#1) {#2};%
    \fi}

\usepackage{tikz-feynman}
\tikzfeynmanset{compat=1.1.0}

% Cancels to number (arrow through with number)
\usepackage{cancel} 

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%% OWN PHYSICS AND MATHEMATICS DEFINITIONS
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\renewcommand{\L}{\mathcal{L}} % Lagrangian density
\renewcommand{\H}{\mathcal{H}} % Hamiltonian density
\newcommand{\psibar}{\overline{\psi}} % Dirac adjoint of field psi
\newcommand{\U}{\mathcal{U}} % Unitary operator
\newcommand{\M}{\mathcal{M}} % Matrix element

\newcommand{\half}{$\nicefrac{1}{2}$\space} % Nice half frac in text
\newcommand{\halfWOspace}{$\nicefrac{1}{2}$} % Nice half frac in text without space at the end, such that it can be used in a sentence before a dot, comma or ect.

% Renewing \exp to contain round parentheses
\newcommand{\pexp}[1]{\exp\left({#1}\right)}
\newcommand{\bexp}[1]{\exp\left[{#1}\right]}
% Alternative way to do it
% \let\oldexp\exp
% \renewcommand{\exp}[1]{\oldexp\left({#1}\right)}

% Renewing \dagger to be a superscript
\let\olddagger\dagger
\renewcommand{\dagger}{^\olddagger}

\newcommand{\Div}[1]{\vgv{\nabla} \cdot #1} % Divergens

\usepackage{physics}